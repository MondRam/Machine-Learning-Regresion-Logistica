#############################
# Variables de entorno (API)
#############################

# URL de la base de datos (PostgreSQL en Neon/otro proveedor)
# Formato SQLAlchemy con psycopg2:
# postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
# Ejemplo (remplaza credenciales y host):
# DATABASE_URL=postgresql+psycopg2://user:pass@ep-xxxx-pooler.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=

# Minado de datos para entrenamiento (no se incluye dataset)
# Opción 1: consulta SQL sobre tu DATABASE_URL que devuelva columnas de features y 'y'
TRAINING_SQL=
# Opción 2: URL externa que entregue CSV o JSON con las columnas requeridas
# También acepta rutas locales (JSON/CSV). Ejemplo en Windows:
# TRAINING_SOURCE_URL=C:\\Users\\tuusuario\\proyecto\\bank-full.json
TRAINING_SOURCE_URL=

# Reentrenamiento automático
# Habilita reentrenamiento en background DESPUÉS de cada predicción (usa fuente minada)
AUTO_RETRAIN_AFTER_PREDICTION=true
# Para activar reentrenamiento basado en feedback etiquetado (si usas /feedback)
AUTO_RETRAIN=false
# Mínimo de ejemplos etiquetados para disparar reentrenamiento (si AUTO_RETRAIN=true)
RETRAIN_MIN_FEEDBACK=1

# (Reentrenamiento desde la UI está deshabilitado en esta versión)

#############################
# Variables para el Dashboard
#############################

# URL base del API (Render u otro host). Para local: http://localhost:8000
# En Streamlit Cloud es preferible usar Secrets, pero esta env var también funcionará localmente.
API_BASE_URL=